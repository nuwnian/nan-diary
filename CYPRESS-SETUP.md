# Cypress setup instructions

1. Install Cypress as a dev dependency:
   npm install --save-dev cypress

2. To open the Cypress test runner:
   npx cypress open

3. To run Cypress tests in headless mode:
   npx cypress run

4. Tests are located in the cypress/e2e/ folder.

5. Configuration is in cypress.config.json.
