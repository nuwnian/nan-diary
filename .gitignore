# Node and editor
node_modules/
.vscode/
.DS_Store
npm-debug.log*
*.log

# Environment variables and secrets
.env
.env.local
.env.production
.env.development
*.key
*.pem

# Firebase
.firebase/
firebase-debug.log

# Build and deployment (contains real API keys)
dist/
deploy/
backend/

# Mobile testing files (may contain API keys during testing)
mobile-test.html

# Temporary files with potential secrets
# Temporary files with potential secrets
*.tmp
*.bak
*-with-keys.html
*-injected.html

# Replacement file produced by scripts/scrub-history.ps1 (may contain plaintext secrets)
replacements.txt

# Additional API key protection
.env.*
!.env.example
!.env.local.example
config-with-keys.*
*-config.js
*-keys.*
service-account*.json
firebase-adminsdk*.json

# Any backup files that might contain keys
*.backup
*.orig
config.backup.*
