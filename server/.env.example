# Server Configuration
PORT=3001
NODE_ENV=development
LOG_LEVEL=info

# Firebase Admin SDK Configuration
# For local development, download your service account JSON from Firebase Console
# and provide the path here. In production, use Application Default Credentials.
FIREBASE_PROJECT_ID=nan-diary-6cdba
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json
# FIREBASE_DATABASE_URL=https://your-project.firebaseio.com

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000

# Security Settings (optional - defaults are provided in config)
# MAX_PROJECTS_PER_USER=100
# MAX_PROJECT_TITLE_LENGTH=200
# MAX_PROJECT_NOTES_LENGTH=50000
